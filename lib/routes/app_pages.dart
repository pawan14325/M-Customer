import 'package:m_customer/routes/app_routes.dart';

class AppPages{
  static const String initialRoute=AppRoutes.splash;
  static  List routes=[
  //   GetPage(
  //  transitionDuration: Duration(milliseconds: 900),
  // name: AppRoutes.splash, page: ()=>),
  ];
}